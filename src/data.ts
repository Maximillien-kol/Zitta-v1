import { Property, SearchProperty } from './types';

// Mock data has been deleted as requested to make the app purely data-driven.
// Added properties will be saved and loaded from Supabase or localStorage.
export const mockProperties: Property[] = [];
export const searchMockProperties: SearchProperty[] = [];
